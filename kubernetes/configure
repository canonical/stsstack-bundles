#!/bin/bash -x
sudo snap install kubectl --classic
mkdir -p ~/.kube
kubemaster_unit=$(juju status| egrep 'kubernetes-master/[[:digit:]]+'| cut -f 1 -d ' ')
juju scp ${kubemaster_unit}:config ~/.kube/config
kubectl --namespace kube-system get pods
