# Variables
debug:                      &debug                     True
openstack_origin:           &openstack_origin          __OS_ORIGIN__

applications:
  magnum:
    charm: __CHARM_STORE____CHARM_CS_NS____CHARM_CH_PREFIX__magnum
    num_units: __NUM_MAGNUM_UNITS__
    constraints: mem=2G
    options:
      debug: *debug
      openstack-origin: *openstack_origin
relations:
  - [ magnum:shared-db, __MYSQL_INTERFACE__ ]
  - [ magnum:identity-service, keystone:identity-service ]
  - [ magnum:amqp, rabbitmq-server:amqp ]
  - [ magnum:certificates, vault:certificates ]
