applications:
  docker:
    charm: __CHARM_STORE____CHARM_NS____CHARM_PREFIX__docker
    channel: __CHARM_TRACK____CHARM_CHANNEL__
relations:
  - [ 'docker:docker', 'kubernetes-worker:container-runtime' ]
  - [ 'docker:docker', 'kubernetes-master:container-runtime' ]
