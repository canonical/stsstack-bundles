applications:
  docker:
    charm: __CHARM_STORE____CHARM_CS_NS____CHARM_CH_PREFIX__docker
relations:
  - [ 'docker:docker', 'kubernetes-worker:container-runtime' ]
  - [ 'docker:docker', 'kubernetes-master:container-runtime' ]
